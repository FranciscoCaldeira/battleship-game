<!DOCTYPE html>
<html>
<head>
    <title>NodeBattleship-Rank</title>
    <meta charset="UTF-8">
    <link rel="stylesheet" href="css/site.css">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
</head>
<body>

<table>
    <tr>
        <th>Nome</th>
        <th>Score</th>
    </tr>
    <% for (var i = 0; i < values.length; i++) { %>
        <tr>
            <td><%= values[i].socket_id %></td>
            <td><%= values[i].score_sum %></td>
        </tr>
    <% } %>
</table>

<%- include('footer')%>  
</body>

